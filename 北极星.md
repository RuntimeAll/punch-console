# 北极星 · 资料产线控制台(punch-console)

> v2 版(2026-08-08,随 PRD-023 立卡重写;v1 北极星的"文件系统是唯一事实源"等条目已被用户拍板推翻,原文见 git 历史)。

## 使命(用户原话)

**解决老师拿不到想要的题的问题;解决老师出不了好题的问题;解决老师备课资料的问题。**

## 一句话定位

六类教学资料(专项/试卷/讲义/练习册/课本/打卡)的**本地生产线 + 可检索知识库**:
手机上看产线、拿发帖物料;题目全部入库可检索;打卡=母题池的举一反三派生,通式可复现。
终局=覆盖 1-9 年级考点的「AI+教辅」随时取用知识库,agent 经 API 取题备课出题。

## 五条地基(2026-08-08 定,四轮设计+40 洞审计倒推)

1. **SQLite 是唯一参照物**(`举一反三产物/资料库.db`)。文案/网盘/状态/题目全在库,md 降级为历史档。
2. **状态算出来,不存两份**:只存 3 个状态字段,泳道/可发布/体检全为现算查询——存了就会过期,过期就是洞。
3. **体检提醒,不拦路**:红灯写在脸上但不锁操作;发布台**拦新不拦旧**(新册要体检全绿,在售老册永远放行)。
4. **搞不定的交给 agent**:Word 拆题/回收、补血缘、转录这类脏活走 session 任务,不造确定性解析器。
5. **Word 是操作台,PDF 是产物**:题目身份=库 id,文件只是投影;版式永远在渲染器,不被 Word 排版污染。

## 六类资料的关系

```
母题池:专项(举一反三产) · 试卷 · 讲义 · 练习册(Word 录入,agent 拆题入库)
参考层:课本(图片 PDF,挂档不拆题)
派生层:打卡 = 对母题做举一反三(mother_id 指母题,generator 记通式,可批量复现)
```

检索三路混合:考点(标签轴)+ FTS5 关键词 + **本地向量语意(RAG 题面轴,bge-small-zh)**。
与线上 PRD-021(pgvector)是本地/线上两条线,互挂指针不合并。

## 三期路线

- **v2(现在,PRD-023)**:Next.js+shadcn+Drizzle 重构;8 表 0 触发器;三路检索;发布台/看板/收件台。
  存量试点=接入《七上有理数混合运算》的**出题算法**(qbank.py 11 生成器进 generator 表,
  直出题带 generator_id+seed 可复现)。🔴 **计算类打卡:资产=生成器,题目=实例**——
  维护的是算法不是题目实例,老成品不转录;其他册生成器随使用逐个接。
- **知识库铺开期**:随生产覆盖考点;考点过百时考点表进库(词表治理);举一反三主产时解题模型表回归(见解层闭合)。
- **终局**:1-9 年级全考点覆盖;agent 经 API 随时取题(备课/订阅特训/出新题先查库);
  确定性类型通式批量直出,不确定类型 agent 按范式出。

## 铁律

- 派 agent 一律 **opus/sonnet,绝不 fable**;任务卡 model 字段锁死。
- **发布永远用户手机人工发**(自动化发小红书=封号红线)。
- 本地先行(CLAUDE.md §0.0):不进部署链、不上公网、无登录;远程需求将来套 Tailscale。
- 实算纪律不许松:红灯题永不放行进产物——知识库的价值=里面每道题都可信。
- 考点名对齐 `举一反三产物/_考点索引/`,这是标签轴不漂移的生命线。

## 演进触发器(按痛点加,不按蓝图加)

| 加什么 | 什么时候 |
|---|---|
| 考点表进库+FTS 扩容 | 考点数过百 / 跨学段生产 |
| 解题模型表回归 | 举一反三成为主要出题方式 |
| L2 结构指纹(仅应用题) | 语意查重漏报真实撞题 ≥3 次 |
| 确定性 Word 回收器 | agent 回收量大到 session 排不过来 |
| 销量 sale 表 | 想记了就加,独立一张不动结构 |
