# 北极星 · 打卡产线控制台

## 一句话

**手机上能看清「哪册走到哪一步」、能一键把该发的文案图片拿走、能随手记一笔销量。**

## 三条边界（越界即跑偏）

1. **文件系统是唯一事实源。** 不建库、不建缓存、不做同步。册目录长什么样，页面就显示什么样；
   页面改的东西也只落回册目录里的 `产线卡.json`。
2. **文案正本是 md，控制台只读。** `_交付/发布物料*.md` 由「每日打卡」skill 产出，改口径去改 md，
   控制台永远是它的**展示面**，不是编辑器。
3. **本地工具，不进系统。** 走 CLAUDE.md §0.0：本地跑通 → 跑久 → 稳定 → 才谈上线。
   在此之前不开 PRD、不动 BE、不进部署链。

## 它替代的是什么

以前查一册要开三个地方：翻 `_工厂索引.md` 看进度、翻 `_交付/发布物料.md` 抄链接和文案、
翻资源管理器找图和 PDF。手机上基本干不了。**现在一页搞定，且是在手机上。**

## 不做什么（想清楚再加）

- 不做题目编辑、不做出题、不做排版——那是「每日打卡」skill 的活。
- 不做多人协作、不做登录鉴权（内网自用，装了就当没有网关）。
- 不做统计报表美学；销量只回答一个问题：**这册到底卖没卖出去。**
- 不自动发布小红书（**自动化发布 = 封号红线**，永远只提供「复制 + 存图」给人手工发）。

## 长大方向（等用久了再说）

- 队列单据真被 agent 消费（现在只是写单，跑不跑还是人在 CLI 里派）。
- 销量从人工记 → 对账导入。
- 产线卡里的「绑定」接上考点索引，做成「这个考点还缺哪册」。

先用够三周，再决定上面哪条值得做。
