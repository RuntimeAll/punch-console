# PRD · punch-console v1 —— 打卡产线本地控制台

> 🔴 **定位说明**:本卡是**本地需求书**,不走调度台账 PRD-NNN 发号(§0.0 本地先行:PRD 卡=上线动作,本项目现阶段不上线)。方向正本 = [北极星.md](北极星.md),本卡只管 v1 做什么、验收线在哪。
> 立卡 2026-08-07 · 用户拍板要点:最快直出、手机可用、去微信化、开源方案不自研。

## 1. 背景与问题

打卡产线(每日打卡 skill + `举一反三产物/打卡/` 目录树)已稳定产出多册,但三件事没有统一入口:

1. **发小红书的物料链路绕远**:电脑上的文案/图 → 复制进微信 → 手机微信收 → 转贴小红书。**要去微信化**。
2. **产线状态散落**:哪册在打样、哪册可发布、哪册在售,只有 `_工厂索引.md` 人读台账,手机上看不了。
3. **销量没有记录位**:卖了多少件、哪个号卖的,无处落账。

## 2. 目标(v1 全部要达成)

| # | 目标 | 验收 |
|---|---|---|
| G1 | 手机浏览器(同一 Wi-Fi)打开 `http://<电脑IP>:8787` 能用全部功能 | 手机实测通 |
| G2 | 每册的小红书物料(标题/正文/话题词/网盘分享语/商品描述/双骨架图)一键拿到 | 三升四基础+提高两册物料页字段齐全 |
| G3 | 产线看板:各册三步走状态 + 销量汇总一屏可见 | 看板列出全部有产线卡的册 |
| G4 | 销量可更新(按版本、按渠道 append) | 提交后写回产线卡.json |
| G5 | 汇总 PDF 导出(勾选多册合并下载) | pymupdf 合并可下载 |
| G6 | 确定性直出与队列下单入口(队列任务卡钉死 `model:"opus"`) | `_队列/待办/` 生成合规 JSON |

## 3. 非目标(v1 明确不做)

- ❌ 不替代正本文档(发布物料正本.md / 产线卡.json 是事实源,页面只是取用口)
- ❌ 不做在线编辑文案(改文案走正本,改完页面自然读到新的)
- ❌ 不做自动发布小红书(封号红线,发布永远人工)
- ❌ 不碰 prod / 不上公网 / 无登录体系(家庭局域网单用户)
- ❌ 不做队列的消费端(v2:agent 领取任务;v1 只下单)

## 4. 用户故事

1. **发帖**:我在手机上打开册详情页 → 复制 A 号文案 → 切小红书粘贴 → 回来长按图存相册 → 发布。全程不经过微信、不开电脑。
2. **记账**:成交一单,手机上找到该册该版本,+1 件、选渠道,提交。看板总销量即时变。
3. **巡线**:打开看板,一眼看到哪册停在「物料 todo」,决定今天补什么。
4. **下单**:想要一册新的应用题打卡,在队列页填册名/考点/天数,任务卡落 `_队列/待办/`,等电脑上的 agent 领取。

## 5. 功能需求

### 5.1 数据层(文件即数据库)

- 每册一张 `<册目录>/产线卡.json`(UTF-8),schema:
  `name / 科目 / 年级 / 绑定{类型:考点|年级, 值[]} / 状态(在产|可发布|在售|停售) / 版本[]`
  版本项:`key / book_id / 网盘{链接,码} / 三步走{打样,全册,物料,录prod: todo|doing|done} / 物料文件 / 图目录{A,B} / 销量[{日期,件数,渠道}]`
- 单版本册也走版本数组(`key:"正册"`);一册多版本、绑考点或绑年级天然支持。
- `科目` 字段 v1 就有(为多科目留位);**考点值必须用 `举一反三产物/_考点索引/` 的考点名**(为 v3 知识库 join 留缝)。
- `bootstrap.py` 铺底:扫 `打卡/` 一级目录(跳 `_` 开头),从 `_交付/发布物料*.md` 解析网盘/book_id/图目录,幂等(`--force` 才重写)。

### 5.2 页面(NiceGUI,移动端优先)

| 页 | 内容 |
|---|---|
| `/` 看板 | 统计条(总销量/在售数) + 科目·年级筛选 + 册卡片(三步走圆点+累计销量) + 汇总PDF入口 + 队列入口 |
| `/book/{name}` 详情 | 版本 tab;每版本:网盘分享语、A/B 文案(标题+正文+话题词)、商品描述——各配复制按钮;图墙(A/B 骨架,点开原图长按保存)+打包 zip;成品 PDF 下载;销量表单+历史;三步走开关 |
| `/queue` 队列 | 下单表单 → `_队列/待办/<时间戳>-<册名>.json`(含 `model:"opus"`,`skill:"每日打卡"`);待办/完成列表 |

### 5.3 关键实现约束

- 🔴 **复制按钮**:HTTP 局域网下 `navigator.clipboard` 被浏览器禁 → try clipboard → catch 走 `execCommand('copy')` 临时 textarea → 再败提示长按全选。
- 静态资源:`PUNCH_ROOT` 挂到 `/data`,中文路径 URL 编码。
- 端口 8787(避开 909x 开发段),`0.0.0.0`,防火墙放行入站 8787。
- 全部文件 IO `encoding='utf-8'`;CLI 脚本开头 `sys.stdout.reconfigure`;print 不带 emoji(GBK 杀进程坑)。
- 依赖只允许 nicegui / pymupdf / requests。

## 6. 技术选型(已拍板)

NiceGUI(开源,pip 装,单文件起,FastAPI 底座——v2 给 agent 加 API 零迁移)。否掉:NocoDB/Baserau(跑不了生成脚本、台账脱钩)、Notion/飞书(云端,违背本地先行)、自研前后端(用户明确否)。

## 7. 里程碑与验收

- M1 代码+铺底:`import` 全通过;三升四铺出基础/提高两版本,eawp/c4gv、双 book_id、四图目录全抓到;铺底幂等。
- M2 冒烟:`/`、`/book/三升四每日一练`、静态图、`/queue` 全 200;队列卡含 `model:"opus"`。
- M3 交付:git master 推 `github.com/RuntimeAll/punch-console`;防火墙放行;服务常驻;**用户手机实测通过 = 收卡**。

## 8. 展望(不在本卡,见北极星)

v2 多科目+手机触发生成(简单计算直出/复杂进队列 agent 领取) → v3 一到九年级数学知识库化(确定性批量直出+agent 按范式直出,考点轴对齐 `_考点索引`)。
